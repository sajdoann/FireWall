/**
* @author Anna Sajdokova <sajdoann@fit.cvut.cz>
* @date 5/11/2020
*/
/*
#pragma once

#include "Patch.h"
#include "MovingInterface.h"

class ShootingPatch : public Patch{

    int range = 0;
    int frequency = 0;
    MovementType movementType;
   // MovementDirection movementDirection;
    MovingInterface movingInterface;

public:
    ShootingPatch(char name, char type, int price, int range, int frequency, MovementType movementType, MovementDirection movementDirection)
    : Patch(name, type, price), range(range),frequency(frequency), movementType(movementType), movementDirection(movementDirection){}

    /** gets the range patch can reach */
    int Range() const { return range; }

    int Frequency()const{ return frequency;}

    /** gets the movement type of the hotfix that can be shooted */
    MovementType MovementTypePatch()const{return movementType;}

    /** gets the movement direction of hotfix that can be shooted */
   // MovementDirection movementDirectionPatch()const{return movementDirection;}

    /** returns true if patch can shoot, this one can -> true */
    bool CanShoot() const{return true;}


    ostream &PrintObject(ostream &os) override {
        return os << (*this).name;
    }





};

